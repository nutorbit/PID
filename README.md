# Proportional Integral Derivative (PID) Control

## Running the code

**Smooth with `P`**

```shell
$ python run_p.py P
```

**Smooth with `PD`**

```shell
$ python run_pd.py P D
```

**Smooth with `PID`**

```shell
$ python run_pid.py P I D
```
